prefix=@DEST_DIR@
exec_prefix=${prefix}
libdir=${prefix}/lib
includedir=${prefix}/include

Name: yokan-server
Description: Mochi yokan server library
Version: @YOKAN_VERSION@

Requires: margo
Libs: -L${libdir} @SERVER_PRIVATE_LIBS@ -lstdc++
Cflags: -I${includedir}
